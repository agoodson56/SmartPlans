{
  "name": "smartplans",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev . --live-reload",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
